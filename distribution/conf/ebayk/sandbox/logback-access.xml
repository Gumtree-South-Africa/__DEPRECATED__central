<configuration>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logDir}/access.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logDir}/access-%d.gz</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%header{X-mobile-source-IP} via %h - %u [%t] %D - "%r" "%header{Host}" "-" %s %b  %header{Referer} "%header{User-Agent}" -</pattern>
        </encoder>
    </appender>

    <appender-ref ref="FILE"/>

</configuration>
